# This is a basic workflow to help you get started with Actions

name: Release Workflow

# Controls when the workflow will run
on:

  # Allows you to run this workflow manually from the Actions tab
  workflow_dispatch:

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      - name: Generate Release Hashes
        # You may pin to the exact commit or the version.
        # uses: MCJack123/ghaction-Generate-Release-Hashes@4e369747b571774842c7bf3bdc4d9513feddf1c0
        uses: MCJack123/ghaction-Generate-Release-Hashes@v2
        with:
          # Type of hash to generate (md5, sha1, sha256, sha512)
          #hash-type: # optional, default is sha256
          # File name to save as if desired
          file-name: hash.txt 
